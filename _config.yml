# --- Site Settings ---
title: "Personalities Database"
description: "Detailed personality profiles of characters from Japanese media, including anime, manga, and light novels."
author: "Characters Wiki"
url: "https://personalitytraits.vercel.app"   # Your Vercel domain
baseurl: ""                                    # Must be empty for Vercel root

# --- Collections ---
# This tells Jekyll to process files in the _personalities/ folder
collections:
  personalities:
    output: true
    permalink: /:name/          # Results in clean URLs like /goku

# --- Layout Defaults ---
defaults:
  - scope:
      path: ""
      type: "personalities"
    values:
      layout: "personality"      # Automatically applies this layout to all personality pages

# --- SEO & Social Metadata ---
social:
  name: "Characters Wiki"
  links:
    - https://github.com/characterswiki/Personalities
twitter:
  username: "characterswiki"
  card: "summary_large_image"
facebook:
  app_id: ""                     # Optional

# --- Build Settings ---
markdown: kramdown
highlighter: rouge
excerpt_separator: ""

# --- Files to Exclude from Build ---
exclude:
  - generate_pages.py
  - .github/
  - _data/
  - README.md
  - Gemfile
  - Gemfile.lock
  - node_modules/
  - vendor/
  - .gitignore
  - vercel.json

# --- Plugins (must be in Gemfile as well) ---
plugins:
  - jekyll-seo-tag
  - jekyll-sitemap
  - jekyll-feed
